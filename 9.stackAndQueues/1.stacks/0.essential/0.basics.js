// Last In First Out (LIFO)
// or
// First In Last Out


/* 
    functions that stack can perform
        1. push(element) ==> Allows us to push something inside the stack
        2. pop() ==> last element come out of the stack
        3. peek() ==> peeks allows us to see which element currently on the top
        4. isEmpty() ==> if the stack is empty of not, returns boolean
        5. size() ==> howmany elements currently inside of the stack
*/

